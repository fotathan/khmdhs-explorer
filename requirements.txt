fastapi==0.139.0   # was 0.115.6; pulls starlette>=1.3.1 (clears PYSEC-2026-161/248/249/1941/1942, CVE-2026-48817/48818)
starlette==1.3.1   # explicit transitive pin so the CVE fix can't drift out under fastapi's range
uvicorn[standard]==0.51.0
jinja2==3.1.6   # was 3.1.5 (PYSEC-2026-1471: sandbox escape via str.format)
psycopg[binary,pool]==3.3.4
requests==2.34.2   # was 2.32.3 (PYSEC-2026-1872 + CVE-2026-25645)
certifi==2026.6.17
openpyxl==3.1.5
pdfplumber==0.11.10
pillow==12.3.0   # was 12.2.0 (PYSEC-2026-2253..2257)
pypdfium2==5.11.0
python-docx==1.2.0
python-multipart==0.0.32   # bumped from 0.0.29 (CVE-2026-53538/53539/53540 in form parsing)
xlrd==2.0.2
httpx==0.28.1
nh3>=0.3.6,<0.4   # HTML sanitiser for curator-authored full_text_html
itsdangerous==2.2.0
boto3==1.43.46      # S3-compatible object storage for act attachments (ATTACHMENTS_BACKEND=s3)
pyotp==2.10.0      # TOTP two-factor auth
segno==1.6.6       # QR codes for TOTP enrollment (pure-python, inline SVG)
