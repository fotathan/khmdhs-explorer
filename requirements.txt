fastapi==0.139.0   # was 0.115.6; pulls starlette>=1.3.1 (clears PYSEC-2026-161/248/249/1941/1942, CVE-2026-48817/48818)
starlette==1.3.1   # explicit transitive pin so the CVE fix can't drift out under fastapi's range
uvicorn[standard]==0.34.0
jinja2==3.1.6   # was 3.1.5 (PYSEC-2026-1471: sandbox escape via str.format)
psycopg[binary,pool]==3.2.3
requests==2.33.0   # was 2.32.3 (PYSEC-2026-1872 + CVE-2026-25645)
certifi==2026.5.20
openpyxl==3.1.5
pdfplumber==0.11.10
pillow==12.2.0
pypdfium2==5.10.1
python-docx==1.2.0
python-multipart==0.0.31   # bumped from 0.0.29 (CVE-2026-53538/53539/53540 in form parsing)
xlrd==2.0.2
httpx==0.28.1
nh3>=0.2.17,<0.3   # HTML sanitiser for curator-authored full_text_html
itsdangerous==2.2.0
boto3==1.43.45      # S3-compatible object storage for act attachments (ATTACHMENTS_BACKEND=s3)
pyotp==2.10.0      # TOTP two-factor auth
segno==1.6.6       # QR codes for TOTP enrollment (pure-python, inline SVG)
